# Installed to /etc/sddm.conf.d/20-virtualkeyboard.conf by
# sddm/install-theme.sh.
#
# An empty InputMethod turns the on-screen keyboard off. qylock's README lists
# it as a known SDDM annoyance: it pops up over the password field by itself,
# and this is a laptop with a real keyboard.
[General]
InputMethod=
