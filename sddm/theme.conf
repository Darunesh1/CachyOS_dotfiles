# Installed to /etc/sddm.conf.d/10-theme.conf by sddm/install-theme.sh.
# The theme itself lives in /usr/share/sddm/themes/ and is fetched by that
# script -- it carries a 39 MB video, so it is not tracked in this repo.
[Theme]
Current=pixel-hollowknight
